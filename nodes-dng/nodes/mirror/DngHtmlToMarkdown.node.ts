import type {
  IExecuteFunctions,
  INodeType,
  INodeTypeDescription,
  INodeExecutionData,
} from 'n8n-workflow';
import { NodeConnectionType, NodeOperationError } from 'n8n-workflow';

export class DngHtmlToMarkdown implements INodeType {
  description: INodeTypeDescription = {
    displayName: "HTML to Markdown",
    name: "dngHtmlToMarkdown",
    group: ['transform'],
    version: 1,
    defaults: { name: "HTML to Markdown" },
    description: "Convert HTML to readable Markdown",
    inputs: [NodeConnectionType.Main],
    outputs: [NodeConnectionType.Main],
    properties: [
      { displayName: "DNG", name: "dngNotice", type: "notice", default: "DNG label: HTML to Markdown" }
    ],
  };

  async execute(this: IExecuteFunctions) {
    const items = this.getInputData();
    if (!items.length) {
      throw new NodeOperationError(
        this.getNode(),
        'DNG mirror nodes need an incoming main connection (Manual trigger or previous DNG node).',
      );
    }
        return await this.prepareOutputData([...items]);
  }
}

export default DngHtmlToMarkdown;

